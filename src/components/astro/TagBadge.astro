---
interface Props {
  tag: string;
  size?: 'sm' | 'md';
  href?: string;
}

const { tag, size = 'md', href } = Astro.props;
const Tag = href ? 'a' : 'span';
const sizeClass = size === 'sm' ? 'text-xs' : 'text-xs';
---

<Tag
  href={href}
  class:list={[
    'inline-block font-normal text-surface-500 dark:text-dm-muted',
    sizeClass,
    href && 'transition-colors hover:text-primary-600 dark:hover:text-accent-400',
  ]}
>
  #{tag}
</Tag>
